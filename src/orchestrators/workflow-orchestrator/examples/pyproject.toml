[project]
name = "examples"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "workflow-orchestrator",
]

[tool.uv.sources]
workflow-orchestrator = { path = "../orchestrator" }
