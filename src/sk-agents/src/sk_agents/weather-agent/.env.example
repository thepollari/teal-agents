# Weather Agent Environment Variables

# Optional: If you want to use OpenWeatherMap API instead of Open-Meteo
# WEATHER_API_KEY=your_openweathermap_api_key_here

# Optional: API timeout settings
# WEATHER_API_TIMEOUT=10

# Optional: Default temperature unit (fahrenheit or celsius)
# TEMPERATURE_UNIT=fahrenheit

# Optional: Default wind speed unit (mph or kmh)
# WIND_SPEED_UNIT=mph

# Note: The current implementation uses Open-Meteo API which is free and requires no API key.
# If you want to extend this agent to use OpenWeatherMap or other APIs that require keys,
# you can add them here and modify the custom_plugins.py file accordingly.
